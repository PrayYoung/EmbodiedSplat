render:
  width: 320
  height: 240

sim:
  dt: 0.1
  step_penalty: 0.01
  goal_radius: 0.5

action:
  type: discrete
  forward_step: 0.15
  turn_deg: 15

task:
  goal_xy: [1.5, 0.0]   # 先写死，后面改成 reset 随机采样
  start_xy: [0.0, 0.0]
  start_yaw_deg: 0
